#pragma once
#include <vector>

inline const int SCREEN_WIDTH = 96;
inline const int SCREEN_HEIGHT = 31;
inline const int OPTIONS_COLUMNS = 2;
inline const int OPTIONS_ROWS = 3;

using render_plane = std::vector<std::vector<bool>>;